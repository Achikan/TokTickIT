# Lab 2 — Visual Inspection & Evidence

> Issue 14 (GitHub #22). This document records the visual inspection outcome against
> `docs/lab-02/ui-spec.md` §7 (Screen States), §12 (Responsive Rules), and §14 (Visual
> Inspection Checklist). Verdicts are based on the implemented code (file references),
> the automated **RESP-01** responsive checks (`e2e/lab-02/responsive.spec.ts`), and the
> Playwright screenshots captured below — not on memory.

## 1. Screenshot Evidence (Playwright)

All screenshots are regenerated by `npm run screenshots` and saved under
`artifacts/lab-02/screenshots/`. Three screens × three viewports (Issue 14):

| Screen | Desktop (1280) | Tablet (820) | Mobile (390) |
|---|---|---|---|
| Create Ticket | `artifacts/lab-02/screenshots/create-ticket/desktop.png` | `.../create-ticket/tablet.png` | `.../create-ticket/mobile.png` |
| My Tickets | `artifacts/lab-02/screenshots/my-tickets/desktop.png` | `.../my-tickets/tablet.png` | `.../my-tickets/mobile.png` |
| Ticket Detail | `artifacts/lab-02/screenshots/ticket-detail/desktop.png` | `.../ticket-detail/tablet.png` | `.../ticket-detail/mobile.png` |

Viewports follow ui-spec §12: desktop ≥ 992 px (multi-column, centered max width),
tablet 768–991 px (two-column where practical), mobile < 768 px (fields stack, no
horizontal scroll).

## 2. Visual Checklist (per ui-spec §14)

Legend: ✅ Pass — evidenced; ⚠️ Deviation / note.

| # | Checklist item (ui-spec §14 / §7 / §12) | Evidence | Verdict |
|---|---|---|---|
| 1 | **Colors/tokens match ui-spec** – primary `#006B3C`, secondary `#0B7A46`, pale `#EAF6EF` | `client/src/styles.css` `:root` tokens (`--tok-primary`, `--tok-secondary`, `--tok-pale`, `--tok-bg`, `--tok-surface`, `--tok-text`, `--tok-readonly`, `--tok-error`, `--tok-warning`, `--tok-success`) asserted by **STYLE-01** (`client/tests/lab-02/style.test.tsx` reads the real `styles.css`); app header uses `#006B3C` (`client/src/App.tsx`), active tab `#0B7A46` | ✅ Pass |
| 2 | **Editable vs read-only distinct** | `.readonly-field` (soft gray-green/ivory) applied to system- and requester-generated values in Create Ticket (`client/src/CreateTicket.tsx`) and Ticket Detail (`client/src/TicketDetail.tsx`); editable fields use `.form-control`/`.form-select` (white, neutral border) per ui-spec §3/§10 | ✅ Pass |
| 3 | **Validation message near field** | `fieldsError.*` messages render directly below each control (`client/src/CreateTicket.tsx` lines 172–250); `is-invalid` class marks invalid fields. Asserted by **STYLE-01** and **UI-01** | ✅ Pass |
| 4 | **Button hierarchy (primary/secondary/destructive/disabled/busy)** | `.btn-tok-primary` (filled green: Continue, Submit), `.btn-tok-secondary` (Change Requester), `.btn-tok-danger` (soft-remove), disabled state (Continue before requester chosen; Submit while busy shows "Submitting…"). Asserted by **STYLE-01** | ✅ Pass |
| 5 | **No clipping / overlap / unintended horizontal scroll** | **RESP-01** asserts `document.documentElement.scrollWidth <= clientWidth` on requester selection, Create Ticket, My Tickets, and Ticket Detail at desktop/tablet/mobile (`e2e/lab-02/responsive.spec.ts`); no unexpected overflow observed in screenshots | ✅ Pass |
| 6 | **Desktop table correct** | My Tickets renders a `<table>` shown at ≥ md (`d-none d-md-block`, `client/src/MyTickets.tsx`) with columns Ticket Number / Summary / Category / Priority / Status / Last Updated; confirmed visually at desktop + tablet | ✅ Pass |
| 7 | **Mobile card / responsive-table correct** | My Tickets renders `<ul>` cards shown below md (`d-md-none`) with ticket number, summary, category, updated time, and badges; buttons remain touch-friendly (RESP-01 click-through at 390 px) | ✅ Pass |
| 8 | **Badges consistent (Requested/IT Priority, Current Status)** | `badge-priority-*` and `badge-status-*` maps in `client/src/MyTickets.tsx` and `client/src/TicketDetail.tsx`; class names asserted by **STYLE-01**; shown consistently in table + cards + detail | ✅ Pass |
| 9 | **Filters, pagination, attachment controls, empty states usable at all sizes** | Search/filters/sort/Reset + pagination in `MyTickets.tsx`; upload/download/remove in `TicketDetail.tsx` `AttachmentSection`; distinct empty ("You don't have any tickets yet") vs no-results ("No tickets match…") states (AC-14). **RESP-01** confirms these remain visible/usable at 1280/820/390 | ✅ Pass |
| 10 | **Screen states present** (initial/loading/empty/no-results/failure, success) | Loading/failure in Create Ticket (`formState`), Requester Selection status (loading/empty/error/ready), My Tickets (loading/ready/failure), Ticket Detail (loading/ready/failure), attachment states in `TicketDetail.tsx` | ✅ Pass |
| 11 | **Application shell + active navigation** | Green `.app-header` (TokTickIT identity), Selected Requester + Change Requester, My Tickets / Create Ticket tabs with `aria-current="page"` on the active view (`client/src/App.tsx`); asserted by **STYLE-01** | ✅ Pass |
| 12 | **Accessibility: labels, focus, non-color indicators** | Visible focus indicators (secondary-green ring) in `styles.css`; `role="alert"` callout for unavailable attachment; success uses text + icon; `prefers-reduced-motion` respected | ✅ Pass |

## 3. Screen-State Coverage

Per ui-spec §7, every screen exposes the required states:

| Screen | Initial | Loading | Empty / No-results | Failure | Success |
|---|---|---|---|---|---|
| Requester Selection | ready dropdown | "Loading development requesters…" | "There are no active development requesters." | "Unable to load development requesters." | – |
| Create Ticket | idle form | reference-data loading | – | "Unable to load reference data" / field errors / API-failure | "Ticket created." + official number + View in My Tickets |
| My Tickets | ready | "Loading your tickets…" | distinct empty vs no-results (AC-14) | "Unable to load your tickets." | – |
| Ticket Detail | ready | "Loading ticket…" | "No attachments yet." | "Unable to load the ticket details." | – |

## 4. Attachment States (ui-spec §6)

Active (list + metadata + Download/Remove), Uploading ("Uploading…" disabled), Invalid
(field-level error for unsupported/oversized), Removed (metadata kept, reason shown,
actions blocked → 410 on download), Unavailable (`.callout-warning` "not available on
the server"). Covered by **API-11..14** and **UI-10**, and visually exercised by
**E2E-02** (`e2e/lab-02/requester-ticket-flow.spec.ts`).

## 5. Additional Report Evidence (Parts 1–3, 6–8)

The screenshots below supplement the Lab report (LEB2 "Answer Part 1–9") and are
captured by dedicated Playwright scripts in `scripts/`:

| Part | Folder | Captures |
|---|---|---|
| 1 (Working methodology) | `artifacts/lab-02/screenshots/part-1-git-evidence/` | commit graph, repo structure, README, .gitignore, PR review table, Issues (all Closed) |
| 1 (Requester states) | `artifacts/lab-02/screenshots/requester-selection/` | loading / loaded / selected |
| 1/6 (Create Ticket states) | `artifacts/lab-02/screenshots/create-ticket-states/` | initial / validation-error / submitting / success / API-failure |
| 2 (Requirement analysis) | `artifacts/lab-02/screenshots/part-2-spec-evidence/` | spec.md existed before implementation PRs (git order) |
| 3 (Testing & traceability) | `artifacts/lab-02/screenshots/part-3-test-evidence/` | full passing server (52) + client (51) outputs |
| 6 (Design documents) | `artifacts/lab-02/screenshots/part-6-evidence/` | UI field ↔ DB column, reference-data seed |
| 7 (My Tickets) | `artifacts/lab-02/screenshots/part-7-my-tickets/` | list, search, filter category/priority/status, sort, pagination, empty, no-results |
| 8 (Ticket Detail + attachments) | `artifacts/lab-02/screenshots/part-8-ticket-detail/` | owned detail, upload, download, soft-remove, blocked-download |
| (Attachment states) | `artifacts/lab-02/screenshots/ticket-detail-attachments/` | initial / valid upload / invalid / soft-removed |
| (Cross-requester security) | `artifacts/lab-02/screenshots/part-78-evidence/` | owner-only list, unauthorized 404 |

## 6. Deviations / Notes

- **Initial Current Status is `SUBMITTED`** in the current implementation
  (`server/prisma/schema.prisma`: `@default(SUBMITTED)`, UI read-only `SUBMITTED`),
  whereas **BR-02 / labsheet §4.3** ("A new Ticket begins with Current Status New")
  requires `New`. This is a **known spec divergence** tracked separately (issue on the
  status workflow) and is **not** a styling defect; screenshots therefore show
  `SUBMITTED`.
- No other visual deviations against ui-spec §14 were found in this inspection.

## 7. How to Reproduce

```bash
# run the app (API on :3000, Vite on :5173) then:
npm run screenshots            # regenerates artifacts/lab-02/screenshots/*
npm run test:e2e               # RESP-01 responsive assertions (no h-scroll, usable controls)
cd client && npm test          # STYLE-01 + UI style assertions
```

Status: **All checklist items pass** except the documented status divergence in §5.
