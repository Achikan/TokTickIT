# Lab 3 — Visual Inspection Checklist (Part 9 Evidence)

Completed against the ui-spec (§8, §11) and the Lab 3 sheet §8.7 at the merged code on
`lab3-staging`. Screenshots are regenerated by `npm run screenshots:lab3` into
`artifacts/lab-03/screenshots/` (27 PNGs — 9 screens × desktop 1280×900 / tablet 820×900 /
mobile 390×844) and are committed as release evidence.

## Checklist

| # | Check | Result | Evidence |
|---|-------|--------|----------|
| 1 | Zen Green tokens/colors match ui-spec §3 (primary `#006B3C`/`#0B7A46`, secondary `#1E6F3B`); badges consistent for Status, Requested Priority, IT Priority and Role | ✅ | `user-management/list-desktop.png`, `staff-queue/queue-desktop.png` |
| 2 | Editable vs read-only fields are visually distinct; validation appears near the field; busy/disabled states correct | ✅ | `authentication/change-password-desktop.png`, `user-management/create-desktop.png` |
| 3 | Role navigation shows only permitted destinations; Logout present in the shell | ✅ | `authentication/shell-desktop.png` |
| 4 | Public Comments vs Internal Notes visually distinct (tint + explicit "Internal" marker), not color alone | ✅ | `staff-ticket-detail/operations-desktop.png` |
| 5 | No clipping, overlap, or unintended horizontal scrolling; queue table and admin list readable and usable at desktop/tablet/mobile | ✅ | RESP-01 Playwright spec (all 5 responsive tests at 3 viewports) + screenshots at each viewport |
| 6 | Loading / empty / no-results / forbidden / failure feedback on all major screens | ✅ | covered by server+client tests; visual feedback also captured in queue/detail screenshots |
| 7 | Keyboard focus indicators visible (box-shadow/outline) and forms keyboard-accessible with labelled inputs | ✅ | A11Y-01 Playwright spec (tab order + focus indicator + landmarks) |
| 8 | Status/role communicated by text, not color alone (screen-reader readable) | ✅ | A11Y-01 + STYLE-02 client tests |

## Screenshot set (`artifacts/lab-03/screenshots/`)

- **authentication/** — `login-{desktop,tablet,mobile}.png` (valid-form login, safe-failure UI),
  `change-password-{...}.png` (mandatory first-login change), `shell-{...}.png`
  (authenticated shell with role badge + Logout).
- **staff-queue/** — `queue-{...}.png` (searchable queue with realistic data, badges, open-detail action).
- **staff-ticket-detail/** — `seeded-details-{...}.png` (seeded ticket with existing comments/notes),
  `operations-{...}.png` (after Claim, IT Priority URGENT, status OPEN, Public Comment,
  Internal Note).
- **user-management/** — `list-{...}.png`, `create-{...}.png`,
  `edit-set-initial-password-{...}.png`.

All 27 files committed under `artifacts/lab-03/screenshots/`, matching ui-spec §11 screenshot
directories.